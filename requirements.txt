# Sugarota Local Flasher Requirements
# ==================================
# No external pip dependencies are required to run the local server!
# The 'run_installer.py' host operates entirely on Python's built-in standard libraries.
#
# To start the configuration dashboard locally:
#   python run_installer.py
#
# To start on a remote headless cloud server/VPS (without GUI browser):
#   python run_installer.py --no-browser
